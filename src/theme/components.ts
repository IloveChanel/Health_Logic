import { StyleSheet } from "react-native";

export const ui = StyleSheet.create({
  buttonPrimary: {
    height: 56,
    borderRadius: 8,
    justifyContent: "center",
    alignItems: "center",
  },

  buttonPrimaryText: {
    fontSize: 14,
    fontWeight: "800",
    letterSpacing: 1,
  },

  card: {
    borderRadius: 12,
    padding: 16,
    borderWidth: 1,
  },
});
